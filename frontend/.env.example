# Leave empty for local dev (Vite proxy handles /api → localhost:3001)
# Set to your deployed backend URL for production builds
VITE_API_URL=

# Supabase — get these from your project's Settings → API page
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
